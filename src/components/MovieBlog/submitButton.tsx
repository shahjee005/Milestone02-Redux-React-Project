import * as React from 'react';

export interface ISubmitButtonProps {
}

export default class SubmitButton extends React.Component<ISubmitButtonProps> {
  public render() {
    return (
      <div>
        stub for submit button
      </div>
    );
  }
}
